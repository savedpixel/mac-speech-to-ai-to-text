# Open Source Name Options

> macOS menu bar app — trigger by shortcut/wake phrase → record speech → transcribe locally with Whisper → clean up with AI → insert into any text field

Must include: **mac** + **trigger/activate** + **record/capture** + **AI cleanup** + **insert/type**

---

## Names

| # | Name | Breakdown |
|---|------|-----------|
| 1 | **MacSpeakForgeInsert** | Mac + speak(record) + forge(AI refine) + insert |
| 2 | **MacTriggerCraftType** | Mac + trigger + craft(AI cleanup) + type(insert) |
| 3 | **MacCaptureCleanInject** | Mac + capture(record) + clean(AI) + inject(insert) |
| 4 | **MacVoxSmithInsert** | Mac + vox(voice/record) + smith(AI craft) + insert |
| 5 | **MacRecordRefineType** | Mac + record + refine(AI) + type(insert) |
| 6 | **MacTriggerSmithType** | Mac + trigger + smith(AI) + type |
| 7 | **MacSpeakPolishInsert** | Mac + speak(record) + polish(AI) + insert |
| 8 | **MacCatchCleanDrop** | Mac + catch(trigger+record) + clean(AI) + drop(insert) |
| 9 | **MacDictateRefineInsert** | Mac + dictate(trigger+record) + refine(AI) + insert |
| 10 | **MacGrabCleanType** | Mac + grab(trigger+record) + clean(AI) + type(insert) |
| 11 | **MacSnapForgeInsert** | Mac + snap(trigger) + forge(AI) + insert |
| 12 | **MacRecordCraftInsert** | Mac + record + craft(AI) + insert |
| 13 | **MacTapCleanType** | Mac + tap(trigger) + clean(AI) + type(insert) |
| 14 | **MacSpeakMintInsert** | Mac + speak(record) + mint(AI refine) + insert |
| 15 | **MacTriggerCleanInsert** | Mac + trigger + clean(AI) + insert |
| 16 | **MacCaptureForgeType** | Mac + capture(record) + forge(AI) + type(insert) |
| 17 | **MacVoiceSmeltInsert** | Mac + voice(record) + smelt(AI extract) + insert |
| 18 | **MacRecordPolishDrop** | Mac + record + polish(AI) + drop(insert) |
| 19 | **MacSpeakBrewInsert** | Mac + speak(record) + brew(AI) + insert |
| 20 | **MacTriggerRefineType** | Mac + trigger + refine(AI) + type(insert) |
| 21 | **MacCatchCraftInsert** | Mac + catch(trigger+record) + craft(AI) + insert |
| 22 | **MacSnapCleanInsert** | Mac + snap(trigger) + clean(AI) + insert |
| 23 | **MacDictateForgeType** | Mac + dictate(record) + forge(AI) + type |
| 24 | **MacRecordSmithDrop** | Mac + record + smith(AI) + drop(insert) |
| 25 | **MacSpeakDistillInsert** | Mac + speak(record) + distill(AI purify) + insert |
| 26 | **MacGrabRefineInsert** | Mac + grab(trigger+record) + refine(AI) + insert |
| 27 | **MacVoxCraftType** | Mac + vox(record) + craft(AI) + type(insert) |
| 28 | **MacTriggerForgeInsert** | Mac + trigger + forge(AI) + insert |
| 29 | **MacCaptureSmithType** | Mac + capture(record) + smith(AI) + type(insert) |
| 30 | **MacSpeakCurateDrop** | Mac + speak(record) + curate(AI) + drop(insert) |
| 31 | **MacRecordDistillType** | Mac + record + distill(AI) + type(insert) |
| 32 | **MacSnapRefineInsert** | Mac + snap(trigger) + refine(AI) + insert |
| 33 | **MacDictateMintType** | Mac + dictate(record) + mint(AI) + type(insert) |
| 34 | **MacCatchForgeType** | Mac + catch(trigger+record) + forge(AI) + type |
| 35 | **MacSpeakSmithType** | Mac + speak(record) + smith(AI) + type(insert) |

---

## Standouts

- **MacRecordRefineType** — clean, plain English, every step obvious
- **MacSpeakForgeInsert** — the "forge" metaphor makes the AI step feel powerful
- **MacCatchCraftInsert** — catch (trigger+record) + craft (AI) + insert — compact and complete
- **MacTriggerCleanInsert** — most literal, zero ambiguity
- **MacSpeakSmithType** — speak → AI smiths it → types it out
