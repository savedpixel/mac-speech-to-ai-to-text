# Task Log — 2026-06-02

<!-- Updated: 2026-06-02 -->

## AI Cleanup Key Persistence Repair

- Investigated Grok changes to the AI cleanup card and found the UI could report a successful DeepSeek connection while the replacement key was not persisted.
- Added Settings-level save/read-back verification and changed the connection card to fail explicitly if Keychain persistence fails.
- Changed Keychain writes to update an existing generic-password item before adding a missing item, avoiding the stale-key path that kept the old invalid suffix visible.
- Bumped the rebuilt app to v1.0.27 (41), packaged/signed/reopened it, and visually verified repeated DeepSeek connection tests plus persistence after Dashboard/Settings navigation and full app relaunch.
- Ran `swift test -j 1`; it is blocked by the current local toolchain because `MacSpeechToAIToTextTests.swift` imports the Apple `Testing` module that is unavailable here. `swift build -j 1` and `scripts/build-app.sh` both passed.
